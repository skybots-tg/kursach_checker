"""Строковые константы, общие для нескольких модулей telegram-интеграции.

Выделены в отдельный файл, чтобы избежать циклического импорта между
``telegram_bot`` и ``telegram_notify`` (последний шлётся из воркера, а
воркер через ``workers.tasks`` → ``telegram_check_handler`` → ``telegram_bot``
инициализируется до окончания загрузки ``telegram_bot``).
"""

# callback_data кнопки «Отправить новый файл» в уведомлении «Готово ✅».
CHECK_UPLOAD_NEW_CB = "check_upload_new"

# payload зарезервированного пункта меню, сообщения которого отправляются
# перед приветствием на /start. Редактируется через админку, вкладка «Тексты».
START_ITEM_PAYLOAD = "__start__"
